#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false); 	cout.tie(0);  cout.tie(0);

using namespace std;
int main()
{
	fast
	double p,q,r;
	cin>>q>>r;
	p=r*q;
	cout<<"product of two number is "<<p;
	return 0;
}
